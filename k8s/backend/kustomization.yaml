apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linkup

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/component: backend
