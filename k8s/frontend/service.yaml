apiVersion: v1
kind: Service
metadata:
  name: linkup-frontend
  labels:
    app: linkup-frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: linkup-frontend
